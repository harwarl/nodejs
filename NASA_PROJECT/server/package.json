{
  "name": "nasa-project-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test-watch": "jest --watch",
    "watch": "set PORT=4000 && nodemon src/server.js",
    "start": "set PORT=4000 && node src/server.js",
    "cluster": "set PORT=4000 && pm2 start src/server.js -i max"
  },
  "author": "oduwale",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.4.3",
    "nodemon": "^2.0.20",
    "supertest": "^6.3.3"
  },
  "dependencies": {
    "express": "^4.18.2",
    "nasa_project": "file:..",
    "pm2": "^5.2.2"
  }
}
